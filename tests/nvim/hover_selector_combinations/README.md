# hover_selector_combinations

## Feature

Hover selector preview section

## Exercise

Requests hover on install-hint and asserts that the static selector preview and truncation marker are present.

## Assumptions

Selector preview remains enabled by the local scenario config.

## Source Under Test

`workspace/locales/es/app.ftl`

This scenario is self-contained. The source fixture and workspace config used by the smoke test live under this directory's `workspace/` tree.
